# 카드뉴스 생성기 — 설계 계획

로컬 LLM(Open WebUI + qwen)으로 첨부 자료에서 카드뉴스를 만드는 데스크톱 앱.
**AI가 만든 티가 나지 않는 것**이 이 프로젝트의 유일한 차별점이다.

## 확정된 결정

| 항목 | 결정 |
|---|---|
| 셸 | Electron (Chromium 내장 → Playwright 제거) |
| 서명 | 일단 무서명 배포, 판매 시점에 재검토 |
| 템플릿 | 실제 카드뉴스 양식 조사 후 5종 사전 탑재 (현재 2종 완료) |
| 이미지 | 첨부 자료에서 추출 우선, 부족분만 생성 |
| LLM의 역할 | **문안만.** 디자인 결정권은 주지 않는다 |
| **배포 대상** | **폐쇄망 윈도우 PC (x64). Open WebUI도 폐쇄망 안에 있다** |
| **배포 방식** | **외부 윈도우 PC에서 빌드 → exe 하나만 반입** |

> 폐쇄망이라 **설치·실행 시점에 네트워크가 전혀 없어야 한다.** 모든 의존물이 exe 안에 들어가고,
> Electron의 맞춤법 사전 다운로드처럼 자동으로 외부를 찌르는 기능은 전부 꺼야 한다.

---

## 1. 왜 Electron인가

지금 구조는 `파이썬 + Playwright + Chromium(150MB)`인데, Electron은 Chromium을 이미 품고 있다.
**Playwright가 통째로 사라진다.** 카드 렌더링은 앱 자기 창(offscreen `BrowserWindow`)에서 하고
`webContents.capturePage()`로 PNG를 뽑는다. 설정 UI도 HTML이라 템플릿 실시간 미리보기가 공짜로 붙는다.

이식 비용은 파서 3종:

| 형식 | 이식 방법 | 난이도 |
|---|---|---|
| PDF | `pdfjs-dist` | 쉬움 |
| hwpx | zip + XML (`fflate` + 정규식) | 쉬움 |
| **hwp** | 기존 파이썬 파서(struct/zlib 40줄)를 Node Buffer로 1:1 이식 | 중간 — 유일한 실질 작업 |
| txt/md | `fs.readFile` | 없음 |

hwp 파서는 이미 검증된 로직이 있으니 새로 연구할 게 없다. `cardnews.py:_hwp_text` 참고.

---

## 2. 카피에서 AI 티 제거

### 2.1 핵심 발견 — 카드뉴스는 "단문"이라 규칙이 반대로 간다

참고한 세 저장소는 전부 **문어체 장문(칼럼·리포트)** 기준이다.
그런데 yoonmoon이 인용한 XDAC 연구(ACL 2025, 한국어 뉴스 댓글 평균 51자)에 따르면
단문에서는 신호 방향이 뒤집힌다. yoonmoon 문서 자체가 "문어체 규칙을 단문에 적용하면 위양성 100%"라고 경고한다.

| 신호 | 장문에서 | 단문(카드뉴스)에서 |
|---|---|---|
| 이모지·장식 | 많으면 AI | 없거나 **획일적**이면 AI |
| 포맷팅(줄바꿈 등) | — | 없으면 AI (사람 10.2% vs AI 0.001%) |
| 어조 | — | 중립·격식이면 AI (사람은 감정 편향) |
| 길이 분산 | 균일하면 AI | 균일하면 AI (유일하게 방향이 같음) |

**따라서 세 스킬을 그대로 파이프에 꽂으면 안 된다.** 단문에 유효한 규칙만 골라 쓴다.

그리고 가장 강한 신호는 단어가 아니라 **길이 분산**이다. AI 카드뉴스는 6장이 전부 비슷한 길이다.
금칙어 목록으로는 절대 안 잡히고, 표준편차 한 줄로 잡힌다.

### 2.2 파이프라인

```
생성(규칙 내장 프롬프트) → 결정론적 린터 → 위반 시에만 1회 수리 호출
```

로컬 qwen은 호출당 수십 초~수 분이다. 무조건 2패스로 돌리면 체감이 두 배가 된다.
린터는 정규식이라 즉시 끝나고, 수리 호출은 "3번 카드가 D-7 위반"이라고 콕 집어주니 근거가 확실하다.
im-not-ai의 light/standard/heavy 라우팅과 같은 발상을 최소 비용으로 구현한 것.

### 2.3 규칙셋 (`rules.json`)

코드가 아니라 데이터로 뺀다. 빌드 없이 사용자가 금칙어를 추가할 수 있고, 스택이 바뀌어도 그대로 쓴다.

**금칙 구문** — 카드뉴스에 압도적으로 자주 나오는 것들 (출처: im-not-ai 카테고리 C·D)

| ID | 패턴 | 예 |
|---|---|---|
| C-8 | 대구법 `A가 아니라 B` | "단순한 도구가 아니라 파트너입니다" |
| D-7 | 변환 공식 `X에서 Y로` | "선택에서 필수로" |
| D-6 | 결말 공식 `~할 때입니다` | "지금이 준비할 때입니다" |
| D-1 | 결산 도입 `결론적으로/정리하자면` | |
| D-4 | hype 어휘 `혁신적/획기적/압도적/필수적/놀라운` | |
| D-2 | 의의 과장 `주목할 만합니다/시사하는 바가 큽니다` | |

**문법 패턴** (카테고리 A·G·I)

- `C-11` 연결어미(-고/-며/-지만/-면서/-아서) 직후 쉼표 — KatFishNet 기준 **4.84배 분리도로 단일 신호 중 최강**
- `A-8` 이중 피동 `되어진다/보여진다`
- `A-9` `~에 의해` 피동 → 행위자를 주어로
- `A-10` `~할 수 있습니다` 남발 → 단언으로
- `I-4` `~해야 합니다/~할 필요가 있습니다`
- `G-1` 추측 종결 `~로 보입니다/~인 듯합니다`

**단문 전용 (XDAC 기반 — 이 프로젝트 고유)**

- 본문 길이 변동계수(stdev/mean) < 0.15 → "너무 균일" 재작성 요구
- 종결어미 4장 연속 동일 → 변주 요구
- 자료에 없는 수치 생성 금지 (`10만 명이 선택한` 류 — kill-ai-slop #29 "invented stat row")

출처: [im-not-ai](https://github.com/epoko77-ai/im-not-ai) (MIT),
[yoonmoon](https://github.com/amondnet/yoonmoon) (MIT),
[kill-ai-slop](https://github.com/yetone/kill-ai-slop) (Apache-2.0). 셋 다 재사용 가능, 출처 표기 예정.

---

## 3. 디자인에서 AI 티 제거

### 3.1 현재 디자인은 슬롭 1번이다

kill-ai-slop의 시각 tell 목록 **1번이 "indigo→violet 대각선 그라데이션(`#6366f1→#a855f7`)"**이고,
현재 [card.html](card.html) 표지가 `#312e81→#7c3aed`다. 6번 "분위기용 그라데이션"에도 걸린다. 폐기한다.

**템플릿 금지 목록** (kill-ai-slop 35개 중 카드뉴스에 해당하는 것)

`#01` 인디고→바이올렛 그라데이션 · `#02` 그라데이션 글자 · `#06` 분위기용 그라데이션 ·
`#15` 이모지 남발 · `#19` 최대 라운드 + 글래스모피즘 · `#20` 과대 그림자 ·
`#25` 자기 색 틴트 아이콘 · `#28` 대문자 카드 그리드 · `#29` 지어낸 통계 행 ·
`#30` 섹션 마커 01/02/03 · `#33` Inter 일변도

### 3.2 2축 분리 — 이게 핵심 구조

조사해보니 카드뉴스는 두 축이 독립적이다. 지금 코드는 이걸 하나로 묶어놔서 모든 카드가 똑같이 생겼다.

**축 1 — 스타일** (색·폰트·질감). 템플릿 전체에 하나. **사람이 고른다.**

**축 2 — 카드별 레이아웃.** 카드마다 다르다. 실제 카드뉴스는 표지·본문·리스트·인용이 전부 다르게 생겼다.

| 레이아웃 | 쓰임 |
|---|---|
| `cover` | 표지. 초대형 제목 + 후킹 |
| `statement` | 소제목 + 설명 (기본형) |
| `list` | 번호 + 항목 3~5개 |
| `quote` | 큰 따옴표 인용 |
| `number` | 초대형 숫자 하나 + 설명 |
| `compare` | 좌우 2분할 (전/후, O/X) |
| `closing` | 요약 + 출처 |

**레이아웃 선택은 LLM에게 맡긴다 — 단, 위 enum 안에서만.**
색이나 폰트를 발명하게 두는 것과 달리, 정해진 7개 중 고르는 건 안전하고 오히려 콘텐츠에 맞다
(인용이 있으면 `quote`, 숫자가 핵심이면 `number`). 그리고 **레이아웃이 섞이는 것 자체가 균일성을 깨서 AI 티를 줄인다.**

구성 흐름은 조사 결과 두 가지로 수렴한다 — **나열형**(도입-정보1..n-정리)과
**스토리텔링형**(why-how-what). 사용자가 고르게 하고 프롬프트를 분기한다.

### 3.3 사전 탑재 템플릿 5종

`templates/<이름>/` 폴더에 `style.css` + `meta.json`. **폴더만 떨구면 템플릿 추가, 코드 수정 없음.**
나중에 템플릿 팩 자체를 따로 팔 수도 있다.

| 템플릿 | 성격 | 폰트 |
|---|---|---|
| **신문 헤드라인체** | 무채색, 굵은 명조, 가는 괘선. 시사·정책 자료에 맞고 AI 티가 가장 적게 남는다 | 본명조 Heavy |
| **단색 초대형 고딕** | 배경 한 색, 글자가 화면을 꽉 채움. 인스타에서 제일 흔하고 제작이 단순 | Pretendard ExtraBold |
| **사진 위 타이포** | 이미지 + 어두운 오버레이 + 흰 글자 | Pretendard |
| **노트 필기체** | 종이 질감 + 손글씨풍 강조 | 나눔손글씨 계열 |
| **매거진 에디토리얼** | 넉넉한 여백, 명조/고딕 혼용, 좌측 정렬 | 본명조 + Pretendard |

**조판 규칙** (조사 결과 공통):
페이지당 색 2~3개 · 행간 = 글자크기 × 1.618 · 여백 충분히 · 본문은 고딕, 제목은 템플릿 재량.

### 3.4 폰트가 사실 제일 큰 레버다

지금은 시스템 폰트(애플 SD 산돌고딕/맑은 고딕)를 쓰는데, 이게 "기본값 느낌"의 정체다.
OFL 폰트를 앱에 넣으면 **카피를 한 글자도 안 고쳐도 AI 티가 눈에 띄게 빠진다.** 카피 규칙 70개보다 이게 세다.

⚠️ **크몽 판매 시 라이선스 확인 필수.** OFL은 임베딩 재배포가 되지만 유료 소프트웨어 번들은 폰트마다 조건이 다르다.
특히 손글씨 폰트(나눔손글씨·교보손글씨 등)는 조건이 제각각이다. 착수 전에 각 폰트 라이선스를 확인해 표로 정리한다.

### 3.5 미리보기 — "내 카드"를 보여준다

템플릿 썸네일에 일반 목업이 아니라 **방금 생성된 1번 카드를 실제로 렌더**해서 띄운다.
Electron이면 offscreen 렌더 한 번이라 거의 공짜고, 고르는 기준이 "예쁜 샘플"이 아니라
"내 글이 저기서 어떻게 보이나"가 된다.

---

## 4. 이미지 — 추출 우선, 부족분만 생성

### 4.1 키가 하나 더 필요 없다

Open WebUI가 이미지 생성을 이미 중계한다. **`POST /api/v1/images/generations`, 인증은 지금 쓰는 그 `sk-` 키 그대로.**

```jsonc
// 요청 (backend/open_webui/routers/images.py: CreateImageForm)
{ "model": null, "prompt": "...", "size": "1024x1024", "n": 1,
  "steps": null, "negative_prompt": "..." }
// 응답
[ { "url": "/cache/image/generations/xxx.png" } ]   // 같은 키로 GET
```

모델 목록은 `GET /api/v1/images/models`로 조회한다.

**백엔드 선택은 우리 앱이 아니라 Open WebUI 관리자 설정에서 한다** — ComfyUI, AUTOMATIC1111, OpenAI(DALL·E), Gemini(Imagen) 중 무엇을 쓰든 우리 코드는 동일하다. 앱은 엔드포인트 하나만 알면 된다.

**사전 조건 (403 나면 이것부터 확인):**
1. Open WebUI 관리자 설정에서 `ENABLE_IMAGE_GENERATION` 켜기
2. 관리자가 아닌 계정이면 `features.image_generation` 권한 필요

⚠️ **백엔드가 클라우드면 로컬 LLM을 쓰는 의미가 반감된다.** 문안은 로컬에서 처리해도 이미지 프롬프트(= 카드 내용 요약)는 OpenAI/Google로 나간다. 남의 문서를 다루는 도구라면 짚고 넘어가야 한다. 완전 오프라인을 유지하려면 ComfyUI 로컬 백엔드를 쓴다. **설정 화면에 현재 백엔드가 로컬인지 클라우드인지 표시한다.**

### 4.2 생성 이미지가 카드뉴스 AI 티의 최대 위험이다

카피 규칙 70개를 지켜도 표지에 번들거리는 DALL·E 그림이 깔리면 3초 만에 들킨다. 그래서 **역할을 나눈다.**

| | 추출 이미지 (자료에서) | 생성 이미지 |
|---|---|---|
| 역할 | **전경 = 내용** | **배경 = 질감** |
| 근거 | 실제 사진·도표라 신뢰도가 붙는다 | 내용을 그리게 하는 순간 AI 티가 난다 |

**핵심 규칙: 생성 이미지는 "주제를 그린 그림"이 아니라 "글자 뒤에 깔 재료"다.**

`사진 위 타이포` 템플릿은 어차피 이미지 위에 어두운 오버레이와 큰 글자가 덮인다. 이미지는 사실상 색면·질감으로만 기능한다. 그러니 처음부터 질감을 만들면 된다 — AI 티의 대부분(어색한 얼굴, 손가락, 깨진 한글, 과포화 색감)을 아예 회피하고, 빠르고 싸다.

**프롬프트 고정 규칙**

- 금지: 글자·문자(한글은 100% 깨진다), 사람 얼굴, 손, 로고, 실존 인물, 특정 브랜드
- 지향: 얕은 심도로 흐린 배경, 질감(종이·콘크리트·직물·물·빛), 단색조, 여백 큰 구도
- `negative_prompt` 활용(A1111·ComfyUI 백엔드): `text, letters, watermark, logo, face, hands, deformed, oversaturated`
- 프롬프트는 **영어 명사구**로. LLM이 카드 JSON을 만들 때 `image_hint` 필드를 같이 뱉게 한다 — **LLM 호출이 늘지 않는다.**

**후처리로 한 벌처럼 만든다 (CSS filter, 공짜)**

추출 이미지와 생성 이미지에 **같은 필터(어둡게 + 채도 낮춤 + 선택적 듀오톤)를 일괄 적용**한다.
실제 편집 디자인이 하는 일이고, 동시에 생성 이미지 특유의 색감을 지운다. 출처가 섞인 티도 사라진다.

### 4.3 추출

| 형식 | 위치 | 상태 |
|---|---|---|
| hwpx | zip 안 `BinData/` | 쉬움 |
| hwp | OLE `BinData` 스트림 (zlib 압축 여부는 헤더 확인) | 중간 — 텍스트 파서와 같은 방식 |
| PDF | `mupdf`(WASM)의 device 콜백 | ✅ **검증 완료** |

**PDF — `mupdf` 1.28.0으로 검증됨.** 사진 1장 + 로고 3회 반복 + 구분선 3회 반복을 넣은 픽스처에서
원본 7개를 잡아 필터 후 사진 1장만 남겼다. **표시 크기(560px)가 아니라 내장 원본 해상도(1200×800)로 나온다** — 카드뉴스에 그대로 쓸 수 있다.

주의할 API 두 가지:

- **ESM 전용.** `require()` 불가, `import`만 된다
- **`mupdf.Device`는 상속이 아니라 콜백 객체를 받는다** (`dist/mupdf.js:1231`).
  `class X extends mupdf.Device`로 만들면 콜백이 하나도 안 불리고 조용히 0개가 나온다

```js
const dev = new mupdf.Device({ fillImage(image) { image.toPixmap().asPNG() },
                               fillImageMask(image) { this.fillImage(image) } });
page.run(dev, mupdf.Matrix.identity); dev.close();
```

**품질 필터** (검증 스크립트: `scratchpad/pdfprobe/extract.mjs`)

순서대로 — **해시 중복 제거를 먼저** 돌려야 같은 로고를 매번 디코드하지 않는다:

1. **해시 중복 제거** — 머리말 로고는 모든 페이지에 반복된다 (픽스처에서 4개 제거됨)
2. 긴 변 800px 미만 버림 (아이콘·로고)
3. 종횡비 4:1 초과 버림 (구분선·배너)
4. 15KB 미만 버림
5. **페이지 전면 스캔 버림** — 이미지 크기가 페이지 크기와 거의 같으면 스캔본이다.
   정부·공공 자료에 흔한데 카드뉴스 소재로는 쓸 수 없다. (픽스처로는 재현 못 함, 실물 스캔 PDF로 확인 필요)

### 4.4 배분과 생성량

이미지가 필요한 레이아웃은 `cover`와 사진형뿐이다. **전체 카드 수가 아니라 이미지가 필요한 카드 수만큼만** 필요하다.

```
필요 장수 = 이미지 필요 카드 수
추출분 우선 배정 (면적 큰 순, 표지에 최고 해상도)
부족분 = 필요 - 추출  →  이만큼만 생성
```

- **생성은 기본 꺼짐.** 체크박스로 켜고, 최대 장수 상한(기본 3장)을 둔다. 클라우드 백엔드면 장당 과금이고 로컬 ComfyUI면 장당 30초~2분이라, 어느 쪽이든 조용히 돌리면 안 된다.
- 켜져 있으면 실행 전에 "N장 생성합니다"를 명시한다.
- 자동 배분은 완벽할 수 없으므로 **결과 그리드에서 카드별 이미지 교체·제거**를 제공한다. 이게 있으면 자동 배분은 대충 맞아도 된다.

### 4.5 카드 스키마 변경

```jsonc
{ "layout": "cover",              // 3.2의 7종 enum 중
  "title": "...",
  "body": "...",
  "image_hint": "blurred concrete texture, low light" }  // 영어, 생성용
```

---

## 5. 앱 구조

```
main            설정 저장, 파일 읽기, LLM 호출(fetch)
  parsers/      pdf.js · hwp.js · hwpx.js · txt.js   (텍스트 + 이미지 동시 추출)
  copy/         prompt.js · lint.js · rules.json
  images/       extract.js · filter.js · generate.js · assign.js
  render/       offscreen BrowserWindow → capturePage → PNG
renderer        설정 화면 / 자료 선택 / 템플릿 갤러리 / 결과 그리드(이미지 교체)
templates/      신문헤드라인/ 단색고딕/ 사진타이포/ 노트/ 매거진/
fonts/          OFL 폰트 (라이선스 확인 후)
```

**설정 화면**: 서버 주소 · API 키 · 모델(`/api/models` 조회 → 드롭다운) · 카드 수 ·
구성 방식(나열형/스토리텔링형) · 톤 · 출력 폴더 ·
**이미지 생성 on/off + 최대 장수 + 이미지 모델(`/api/v1/images/models`) + 백엔드 로컬/클라우드 표시**.
`electron-store`로 저장하고 API 키는 `safeStorage`로 암호화한다
(현재 파이썬 버전은 `config.json` 평문이라 이때 해결된다).

---

## 6. 작업 순서

규칙은 언어 중립 데이터라 스택과 무관하다. **스택을 정했으니 이제 한 번만 만든다.**

규칙은 언어 중립 데이터라 스택과 무관하다. **스택을 정했으니 이제 한 번만 만든다.**

### 완료 (구현됨)

| | 검증 |
|---|---|
| PDF/HWP/HWPX/TXT 파서 + 이미지 추출 + 품질 필터 | `npm run selftest` 18개 통과 |
| `rules.json` + 린터 (금칙 구문 · 균일성 · 종결어미) | 〃 |
| Electron 셸 · 설정 UI · 템플릿 갤러리 | `test/ui-test.mjs` 통과 |
| 레이아웃 7종 + 템플릿 2종 | `test/render-test.mjs` — 14장 렌더 확인 |
| 이미지 추출→배분→부족분 생성 파이프라인 | 배분·필터는 검증, **생성 호출은 서버 없어 미검증** |
| 윈도우 x64 빌드 + 반입 패키지 | 설정 검증(`--dir` 빌드 성공), **NSIS 설치 파일은 윈도우에서 확인 필요** |

### 남은 것

0. **Open WebUI 실호출 검증** — 폐쇄망 PC에서 앱을 설치한 뒤 확인해야 한다. 앱의 "목록 조회"·"확인" 버튼이 그 역할을 한다
1. ~~`rules.json` + 린터~~ ✅
2. **린터 임계값 튜닝** — 실제 qwen 출력으로 오탐/미탐 확인
3. **템플릿 3종 추가** — 사진 위 타이포 / 노트 필기체 / 매거진 에디토리얼
4. **폰트 라이선스 조사 후 번들** — `fonts/README.md` 참고. 효과가 가장 큰 항목
5. **결과 그리드에서 이미지 교체** — 자동 배분이 어긋났을 때의 탈출구
6. **윈도우 실기 검증** — 설치·실행·한글 폰트 폴백·SmartScreen 문구

<details><summary>원래 순서 (참고)</summary>

1. **`rules.json` + 린터** — 지금 파이썬으로 먼저 검증 가능. 실제 AI 카드뉴스 문안을 넣어 탐지율 확인
2. **폰트 라이선스 조사** — 5종 템플릿에 쓸 폰트 확정 (판매 가능 여부 포함)
3. **Electron 셸** — 설정 화면 + 파서 이식 + LLM 호출까지 (기존 파이썬과 동일 기능 재현)
4. **레이아웃 7종 + 템플릿 5종** — 가장 오래 걸리는 단계
5. **이미지 파이프라인** — 추출 → 필터 → 배분 → 부족분 생성 → 일괄 필터
6. **템플릿 갤러리 + 실시간 미리보기 + 결과 그리드 이미지 교체**
7. **electron-builder 빌드** — 무서명 `.dmg` / `.exe`, 경고 우회 안내 문서 동봉

0번과 1번은 지금 바로 할 수 있고 나머지의 전제라서 먼저 한다.

</details>

---

## 7. 미결 사항

- **스캔본 PDF 대응.** 4.3의 필터 5번은 실물 스캔 PDF가 없어 미검증이다. 스캔본이면 이미지 소재가 0개가 되므로 전량 생성으로 넘어간다.
- **폰트 번들 라이선스** — 3.4 참고. 판매 계획이 확정되면 더 보수적으로 골라야 한다.
- **추출 이미지의 저작권** — 사용자가 넣은 자료의 사진을 카드뉴스에 그대로 싣는 건 사용자 책임이다.
  판매 도구라면 결과 화면에 한 줄 고지가 필요할 수 있다.
- **무서명 배포 안내** — 맥 우클릭→열기, 윈도우 SmartScreen 우회를 어디서 안내할지(README? 첫 실행 화면?).
- **린터 임계값 튜닝** — 변동계수 0.15는 XDAC의 IQR 수치에서 역산한 초기값이라 실측으로 조정해야 한다.
